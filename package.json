{
  "name": "square-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply square-web --local",
    "db:migrate:remote": "wrangler d1 migrations apply square-web --remote",
    "data:export": "node scripts/export-mysql.mjs",
    "data:convert": "node scripts/generate-d1-import.mjs",
    "images:upload": "node scripts/upload-r2-images.mjs",
    "secrets:generate": "node scripts/generate-secrets.mjs"
  },
  "dependencies": {
    "hono": "^4.9.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "mysql2": "^3.14.0"
  }
}
