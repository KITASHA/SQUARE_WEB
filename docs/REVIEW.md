# 再構築の検証記録

2026-09-18。紹介ページとリンクのみを残し、文章と既存デザインを維持する今回の方針に合わせて再構築。

## 追加指示による変更

- 公開ページを6ページに変更。発声ワークショップ動画のカードとページを廃止し、旧URL・.html・配下URLはトップへ301転送。
- トップに定期活動会（show_1）とスターターバンド制度（show_3）のカードを追加。SQUAREについて・show_1・show_2・show_3・入会について・YouTubeの順で6枚を配置。
- 既存カードと同じBootstrapデザインを使用。紹介ページ本文とCSSは維持。
- 初回の比較基準は変更せず、テストで廃止ページを除外し、承認済みカードの期待値を独立して定義。

## 追加指示後の検証

- `node --test tests/content.test.mjs tests/routes.test.mjs` の12テスト成功。6ページの内容、トップの6カード、元画像・CSS、廃止URLの転送、書込み405、HEAD、404を検証。
- ローカルWranglerの `http://127.0.0.1:8787` で、`verify-preview.mjs` による6ページ・21個の画像/CSS/JS/フォント等の実HTTP検証と転送確認が成功。
- ブラウザでPC幅1440pxの3列2段、スマホ幅390pxの1列表示を確認。全6カードと画像は正常。

## 初回再構築時の検証記録（過去）

以下はワークショップ廃止とカード追加より前の7ページ構成に対する記録です。今回の変更後の検証結果を示すものではありません。

- 元の本番HTMLと旧Railsテンプレートを照合し、独立した比較基準を作成。
- 7ページの文章、リンク、画像属性、Bootstrapクラスとinline styleの一致を確認。
- 使用中の13画像と旧CSSのSHA-256一致。旧21画像は削除せず保持。
- 13個の自動テスト成功。旧URL301、書込み405、HEAD、404、DB/R2依存なしも検証。
- 構文チェック、ESLint成功。
- Wrangler 4.134.0でデプロイdry-run成功（ASSETSとPREVIEWのみ）。
- ローカルWranglerで7ページ・22個の画像/CSS/JS/フォント等を実HTTP検証。レスポンス本文とファイルハッシュ一致。
- ブラウザで幅1440px・390pxの全7ページを確認。読込完了後の横はみ出し・画像切れなし。
- メニュー開閉、ContactのSNSリンク、ワークショップ動画リンクを確認。
- npm install監査の脆弱性0件（古いWranglerの開発依存を更新）。
- 独立レビューで今回要件に対するブロッカーなし。

## 再実行

```sh
npm ci
npm run validate
npm run deploy:dry-run
npm run dev
# 別ターミナル
node scripts/verify-preview.mjs http://127.0.0.1:8787
```

## 初回記録時の公開状態

この記録作成時点ではローカル確認まで完了。workers.devへの公開と実機スマホでの確認はまだ未完了です。Cloudflareの認証状態を確認してから仮公開します。

本番ドメイン・DNS・Lightsail・MySQL・旧Railsリポジトリには変更を加えていません。アプリ用SecretsやDB・R2の準備は不要です。
