# SQUARE Webサイト／バンド管理システム 保守・運用資料

最終更新: 2026-09-22

## 1. この資料の目的

この資料は、SQUARE公式Webサイトとバンド管理システムを、将来修正・復旧・引き継ぎするための保守資料です。
こちらは実際にトラブル対応や仕様変更を行う人向けの詳細版です。

> シークレット値・パスワード・APIトークンそのものは記載しません。

---

## 2. システム全体構成

バンド情報は次の順で流れます。

```text
Google Forms
    ↓
Google Sheets
    ↓
Google Apps Script
    ↓
Cloudflare Worker
    ↓
公式Webサイト
```

バンド画像は次の経路です。

```text
Google Forms
    ↓
Google Drive
    ↓
Google Apps Script
    ↓
Cloudflare Worker
    ↓
Cloudflare R2
    ↓
Worker経由で画像配信
```

基本方針は、管理者が毎回手作業で更新するのではなく、バンド側が自分で登録・編集・画像変更・削除できるようにすることです。

---

## 3. GitHub / Webサイト

### リポジトリ

```text
https://github.com/KITASHA/SQUARE_WEB
```

### 主な構成

```text
public/
  静的Webサイト

src/index.js
  Cloudflare Worker
```

フロントエンドは基本的に以下で構成しています。

- HTML
- CSS
- JavaScript

特定のフレームワークには依存しない構成です。

---

## 4. Cloudflare

### アカウント

Cloudflareアカウント名:

```text
SQUARE
```

現在の管理ユーザー:

```text
square.okayama@gmail.com
```

### Worker

現在のWorker名:

```text
square-web-staging
```

主なAPI:

```text
POST /api/bands-sync
POST /api/band-image-ingest
GET  /media/bands/...
```

### R2

バンド画像用バケット:

```text
square-band-images
```

R2バケット自体は公開せず、Worker経由で画像を配信します。

### Apps Script → Worker 認証

Apps Script側の Script Properties:

```text
CLOUDFLARE_INGEST_SECRET
```

Worker側:

```text
BAND_INGEST_SECRET
```

両者は同じ共有シークレットを使います。

#### 注意

- シークレット値をGitHubへコミットしない
- READMEや保守資料にも実値を書かない
- 値を変更する場合はApps Script側とWorker側を同時に変更する

---

## 5. Google Spreadsheet

スプレッドシート名:

```text
SQUARE バンド登録管理
```

Spreadsheet ID:

```text
1S5ZumKvxnhfixxb1OSr4NBflZOfSLQU7Hy4mcjWIAok
```

### 使用するシート

#### `バンド登録回答`

バンド情報の正本です。

サイト公開データ、バンド参照、画像情報などは、このシートを基準に生成します。

#### `画像変更回答`

画像変更フォームの回答履歴です。

#### `バンド削除回答`

削除フォームの回答履歴です。

#### `バンド参照`

管理者向けの見やすい一覧です。

#### `設定`

フォームID、フォームURL、回答シート名などの内部設定を保持します。

### 旧環境のシート

```text
Form Responses 1
Form Responses 2
```

新環境では使用しません。

完全移行後は削除可能です。

---

## 6. Google Forms

### 6.1 SQUARE バンド登録

Form ID:

```text
1WkdfGMQBmIFCvVMMYTfI_W1Kj5mqdhs1Jqie_nV7Jww
```

編集URL:

```text
https://docs.google.com/forms/d/1WkdfGMQBmIFCvVMMYTfI_W1Kj5mqdhs1Jqie_nV7Jww/edit
```

公開URL:

```text
https://docs.google.com/forms/d/e/1FAIpQLScpOA1rUU1kHlLFXdbdxGRWqd-_ORlylz4ILSgKIiq3xOWiqw/viewform
```

主な設問:

- バンド名
- メンバー1
- メンバー2
- メンバー3
- メンバー4
- メンバー5
- メンバー6
- メンバー7
- メンバー8
- 紹介文
- バンド画像
- X
- Instagram
- リンク名①
- その他リンク①
- リンク名②
- その他リンク②
- 活動休止中

必須項目:

- バンド名
- メンバー1
- 紹介文

### 6.2 SQUARE バンド画像変更

Form ID:

```text
1KFveCZcYlLQEOviMVNdNEWx6O6zzTgHKOzMRdF-U9uo
```

編集URL:

```text
https://docs.google.com/forms/d/1KFveCZcYlLQEOviMVNdNEWx6O6zzTgHKOzMRdF-U9uo/edit
```

公開URL:

```text
https://docs.google.com/forms/d/e/1FAIpQLSfJm_aYvXRkEeqS6AyfQ3umx5vjQWLv199aKwNUrWp5jeJ1sw/viewform
```

主な設問:

- 回答ID
- 変更バンド画像

`回答ID` はプリフィルURLから自動入力される前提です。

利用者が手動変更しないようにします。

本人確認は、登録時メールアドレスと送信者メールアドレスの一致で行います。

### 6.3 SQUARE バンド削除

Form ID:

```text
1ug7QigwpiPQEy3A1px6Y6I52sPahaVEtee6y0w43SCU
```

編集URL:

```text
https://docs.google.com/forms/d/1ug7QigwpiPQEy3A1px6Y6I52sPahaVEtee6y0w43SCU/edit
```

公開URL:

```text
https://docs.google.com/forms/d/e/1FAIpQLSd2yXVsfOnCDJdsxDfAY6WRgc5ILjC0kz2eixLafTmiVPAziA/viewform
```

主な設問:

- 回答ID
- 削除確認

削除確認の選択肢:

```text
このバンドを完全に削除する
```

本人確認は、登録時メールアドレスと送信者メールアドレスの一致で行います。

---

## 7. `バンド登録回答` の管理列

フォーム回答列の後ろに、Apps Scriptが以下の管理列を使用します。

- 回答ID
- 公開ID
- 編集URL
- 画像変更URL
- 削除URL
- Drive画像ファイルID
- R2画像URL
- R2オブジェクトキー
- 画像処理状態
- 最終更新日時
- 初回URL送信日時

### 回答ID

Google Form側の回答IDです。

編集・画像変更・削除対象の特定に使用します。

**手動変更禁止。**

### 公開ID

サイト公開用の安定IDです。

行番号ではなく、このIDでバンドを識別します。

**手動変更禁止。**

### 編集URL

元のバンド登録回答を編集するURLです。

### 画像変更URL

回答IDを埋め込んだ画像変更フォームURLです。

### 削除URL

回答IDを埋め込んだ削除フォームURLです。

### Drive画像ファイルID

Google Formsでアップロードされた画像のDriveファイルIDです。

### R2画像URL

サイトで使用する画像URLです。

### R2オブジェクトキー

R2内の画像キーです。

画像差し替え・削除時に使用します。

### 画像処理状態

主な値:

```text
画像なし
R2転送中
完了
画像変更中
失敗: ...
画像変更失敗: ...
```

### 初回URL送信日時

新規登録時に管理URLメールを送信済みか判定するために使用します。

---

## 8. `バンド参照` シート

管理者向けの一覧です。

列構成:

| 列 | 内容 |
|---|---|
| バンド名 | 登録バンド名 |
| メンバー | メンバー1〜8をまとめて表示 |
| 登録メール | 登録時のメールアドレス |
| 状態 | 活動中 / 活動休止中 |
| 最終更新日時 | 最後に更新された日時 |
| 画像処理状態 | R2画像処理の状態 |
| URL再送 | 管理URL再送用チェックボックス |
| 再送結果 | 送信結果またはエラー |
| 公開ID | 内部管理用。通常は非表示 |

メンバーは空欄を除き、以下のように1セルへまとめます。

```text
メンバーA / メンバーB / メンバーC
```

### URL再送

`URL再送` にチェックすると、登録者へ以下をまとめて送信します。

- バンド情報編集URL
- バンド画像変更URL
- バンド削除URL

送信後:

- チェックは自動でOFF
- `再送結果` に送信日時またはエラーを記録

> `バンド参照` は管理用の派生データです。  
> 正本は必ず `バンド登録回答` です。

---

## 9. Apps Script ファイル構成

現在は6ファイル構成です。

### `SETUP.gs`

初期構築・開発用です。

主な役割:

- Google Forms作成
- 回答シート初期構築
- バンド参照初期構築
- トリガー作成

通常運用では基本的に実行しません。

特に以下は運用開始後に不用意に実行しないでください。

```javascript
setupStep1CreateForms();
setupStep2Finalize();
```

フォームや回答先シートを再構築する処理のため、運用データがある状態での再実行は事故につながります。

### `MAIN.gs`

トリガーの入口です。

主な関数:

```javascript
onFormSubmit(e)
onImageChangeFormSubmit(e)
onDeleteFormSubmit(e)
onReferenceEdit(e)
mainSyncAll()
```

### `BAND.gs`

バンドデータ処理本体です。

主な役割:

- 新規登録
- 回答編集
- 初期画像登録
- 画像変更
- 完全削除
- 活動休止判定
- バンド参照更新
- Webサイト同期
- 公開ID管理
- メンバー整形
- その他リンク生成

### `MAIL.gs`

メールと管理URLを扱います。

主な役割:

- 新規登録完了メール
- 編集URL
- 画像変更URL
- 削除URL
- 管理URL再送
- プリフィルURL生成

### `R2.gs`

Cloudflare R2画像処理です。

主な役割:

- Google Drive画像をR2へ保存
- 既存画像差し替え
- R2画像削除
- Workerレスポンス判定
- Cloudflare共有シークレット取得

### `UTIL.gs`

共通処理です。

主な役割:

- 設定シート取得
- Spreadsheet取得
- ヘッダーマッピング
- セル読み書き
- 回答行検索
- Google Form回答取得
- ファイルID抽出
- メールアドレス正規化
- 本人確認
- 活動休止判定
- 排他制御

---

## 10. トリガー

必要なインストール型トリガー:

| 対象 | 関数 |
|---|---|
| バンド登録フォーム | `onFormSubmit` |
| 画像変更フォーム | `onImageChangeFormSubmit` |
| 削除フォーム | `onDeleteFormSubmit` |
| スプレッドシート | `onReferenceEdit` |

トリガーが消えた場合は `SETUP.gs` の以下を使用します。

```javascript
setupInstallTriggers();
```

正常動作中に何度も実行する必要はありません。

トリガー障害時はApps Scriptの「トリガー」と「実行数」から失敗履歴を確認します。

---

## 11. 新規登録時の処理

新しいバンドが送信されると、以下の順で処理します。

1. Google Forms回答を取得
2. 回答シート上の対応行を特定
3. 回答IDを保存
4. 公開IDを発行
5. 編集URLを保存
6. 画像変更URLを生成
7. 削除URLを生成
8. 登録者へ管理URL3種類をメール送信
9. バンド画像があればR2へ転送
10. バンド参照を更新
11. 公開バンド一覧をWorkerへ同期
12. 公式サイトへ反映

初回メールは `初回URL送信日時` が空の場合のみ送ります。

回答編集のたびに同じメールは送りません。

---

## 12. バンド情報編集時の処理

編集URLから登録内容を変更すると:

1. 同じ回答IDの既存行を特定
2. Google Forms側の最新回答内容が回答シートへ反映
3. 最終更新日時を更新
4. バンド参照を更新
5. Workerへバンド一覧を再同期

### 重要: 通常編集では画像を処理しない

Google Formsの元回答には、最初にアップロードした画像が残ります。

通常編集時に画像まで再処理すると、専用画像変更フォームで変更した新画像を、古い画像で上書きする可能性があります。

そのため、

> **画像変更は必ず専用フォームで行う**

という設計です。

---

## 13. 活動休止

`活動休止中` にチェックが入っている場合:

- `バンド登録回答` には残る
- `バンド参照` にも残る
- R2画像も残る
- 編集URLも有効
- 公式サイト公開データからだけ除外

チェックを外すと、次回同期時に再びサイトへ表示されます。

---

## 14. 画像変更時の処理

画像変更フォーム送信時:

1. 回答IDを取得
2. 登録バンドを検索
3. 登録メールと送信者メールを比較
4. 新しいDriveファイルIDを取得
5. Cloudflare Workerへ画像保存要求
6. R2画像を更新
7. 新しいR2 URL / Keyを回答シートへ保存
8. バンド参照更新
9. サイト再同期

メールアドレス不一致時は処理を止めます。

---

## 15. 完全削除時の処理

削除フォーム送信時:

1. 回答IDを取得
2. 削除確認を確認
3. 登録バンドを検索
4. 登録メールと送信者メールを比較
5. Google Forms本体の回答を削除
6. `バンド登録回答` の対象行を削除
7. バンド参照を更新
8. サイト公開データを再同期
9. R2画像を削除

Google Formsの回答を削除しても、回答先スプレッドシートの行は自動では消えないため、シート側も明示的に削除します。

---

## 16. Webサイトへ送る公開データ

主なJSON構造:

```json
{
  "id": "公開ID",
  "name": "バンド名",
  "members": [
    "メンバー1",
    "メンバー2"
  ],
  "description": "紹介文",
  "x": "X URL",
  "instagram": "Instagram URL",
  "otherLinks": [
    {
      "label": "リンク名",
      "url": "URL"
    }
  ],
  "imageUrl": "R2画像URL"
}
```

活動休止中のバンドは、この公開一覧へ含めません。

### その他リンク

最大2件。

リンク名が空欄の場合:

```text
その他リンク
```

---

## 17. `設定` シートの主なキー

主なキー:

```text
SQUARE_SPREADSHEET_ID

SQUARE_MAIN_FORM_ID
SQUARE_MAIN_FORM_EDIT_URL
SQUARE_MAIN_FORM_PUBLIC_URL
SQUARE_MAIN_RESPONSE_SHEET

SQUARE_IMAGE_FORM_ID
SQUARE_IMAGE_FORM_EDIT_URL
SQUARE_IMAGE_FORM_PUBLIC_URL
SQUARE_IMAGE_RESPONSE_SHEET

SQUARE_DELETE_FORM_ID
SQUARE_DELETE_FORM_EDIT_URL
SQUARE_DELETE_FORM_PUBLIC_URL
SQUARE_DELETE_RESPONSE_SHEET

SQUARE_REFERENCE_SHEET

SQUARE_SETUP_VERSION
```

同じキーが複数存在する場合、現行コードは下側にある値を優先して取得します。

---

## 18. 手動同期

バンド参照と公式サイトをまとめて再同期する場合:

```javascript
mainSyncAll();
```

使用例:

- コード変更後
- バンド参照を作り直したい
- サイト同期が途中で失敗した
- 復旧作業後

通常運用では自動同期されるため、常用する必要はありません。

---

## 19. 障害時の確認ポイント

### バンド登録したのにサイトに出ない

確認順:

1. `バンド登録回答` に回答があるか
2. `活動休止中` になっていないか
3. `公開ID` が入っているか
4. Apps Scriptの実行履歴にエラーがないか
5. `syncBandsToWebsite` 系処理が失敗していないか
6. Workerの `/api/bands-sync` が正常か

### 画像が出ない

確認順:

1. `Drive画像ファイルID`
2. `R2画像URL`
3. `R2オブジェクトキー`
4. `画像処理状態`
5. R2バケット `square-band-images`
6. Workerの `/media/bands/...`

### 登録メールが届かない

確認順:

1. `初回URL送信日時`
2. Apps Script実行履歴
3. 登録メールアドレス
4. Gmailの迷惑メール
5. `MAIL.gs` の送信処理

### URL再送が動かない

確認順:

1. `バンド参照` の `URL再送` 列か
2. `onReferenceEdit` トリガーが存在するか
3. 公開IDが対象バンドと一致するか
4. `再送結果` にエラーが出ていないか

### 画像変更できない

確認順:

1. プリフィルされた回答ID
2. 登録時メールアドレスとの一致
3. 新しいファイルが取得できているか
4. Worker / R2のエラー

### 削除できない

確認順:

1. 回答ID
2. 削除確認
3. 登録時メールアドレスとの一致
4. `onDeleteFormSubmit` トリガー
5. Apps Script実行履歴

---

## 20. 仕様変更時の注意

### Google Formsの質問名を変更する場合

Apps Scriptは設問タイトルを文字列で参照しています。

例:

```text
バンド名
メンバー1
紹介文
バンド画像
活動休止中
変更バンド画像
回答ID
削除確認
```

フォーム側だけ名称変更すると処理が壊れる可能性があります。

必ずコード側の参照も確認してください。

### 回答シートの列

Google Formsが管理している回答シートの列は、手動で並べ替えないでください。

管理列はフォーム回答列の後ろへ追加します。

### `回答ID` / `公開ID`

どちらも内部識別に使用するため、手動変更しないでください。

---

## 21. バックアップ・復旧

大きな変更前には以下を推奨します。

1. GitHubへ現在のコードをcommit
2. Apps Scriptコードの状態を確認
3. スプレッドシートのバックアップを作る
4. 本番フォームを不用意に再構築しない
5. シークレット値を別途安全な場所で管理する

問題が発生した場合は、まずデータを削除せず、Apps Scriptの実行履歴と回答シートを確認します。

---

## 22. 保守時に触る可能性が高い場所

### サイトデザイン変更

```text
public/
```

### Worker / API変更

```text
src/index.js
```

### バンド登録・同期ロジック

```text
BAND.gs
```

### メール文面・管理URL

```text
MAIL.gs
```

### 画像保存・削除

```text
R2.gs
```

### 共通処理・設定取得

```text
UTIL.gs
```

### トリガー・初期構築

```text
SETUP.gs
```

---

## 23. 運用上の重要ルール

- `バンド登録回答` を正本として扱う
- `バンド参照` は管理用の派生シート
- 回答シートの列を手動で並べ替えない
- `回答ID` と `公開ID` を変更しない
- 画像変更は専用フォームを使う
- 活動休止は削除ではない
- SETUPの初期構築関数を運用中に不用意に実行しない
- シークレットをGitHubへ保存しない
- 大きな変更前にGitへcommitする

---

## 24. 最低限の動作確認チェックリスト

大きな変更後は、最低限以下を確認します。

- [ ] 新規バンド登録
- [ ] サイトへ表示
- [ ] 登録完了メール受信
- [ ] 編集URLから内容変更
- [ ] 編集内容がサイトへ反映
- [ ] 活動休止ONでサイト非表示
- [ ] 活動休止OFFで再表示
- [ ] 画像変更
- [ ] URL再送
- [ ] 完全削除
- [ ] R2画像削除
- [ ] `バンド参照` 更新

この一連が通れば、管理システム全体が正常に連携していると判断できます。
